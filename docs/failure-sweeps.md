# Failure sweeps: pre-flight guards against classes already hit

Every failure class below was hit at least once against the real GPU box or
a live browser, at a cost of a ~10-minute box boot (or worse) per incident.
Each row is: what happened -> what the codebase-wide sweep found -> the
permanent guard now in place (a test, unless noted).

| Class | What happened | Sweep result | Guard now in place |
| --- | --- | --- | --- |
| A -- embedded code untested | `remote_job.sh`'s inline `uv run python -c "..."` snippets called `enc.encode(clip.frames)` against a real `encode(clip: Clip)` signature -- crashed only on the box. | Confirmed these snippets are the ONLY embedded-python call sites in `scripts/aws/*.sh`; no other `.sh` file embeds python. Follow-up review found the original guard only checked call ARITY (`inspect.signature`), which is blind to `enc.encode(clip.frames)` vs `enc.encode(clip)` -- both are exactly one positional argument. Reproduced the original bug standalone in a scratch `remote_job.sh` and confirmed the arity-only guard passed vacuously. | `tests/test_embedded_scripts.py` extracts every `uv run python -c "..."` snippet, parses it with `ast`, resolves calls (including one level of method chaining, e.g. `DINOv2Encoder().load()`) against real `physics_auditor` objects, checks call arity via `inspect.signature`, AND does a type/shape check: if a local variable is already known (via return-type-annotation inference, including string forward refs) to be exactly the type a parameter is annotated with, but the argument passed is `that_local.some_attr` instead of `that_local` itself, that's flagged even though arity matches. Re-verified against the scratch repro: now caught. |
| B -- mocks pin the wrong external API | Test mocks accepted `model(pixel_values=...)`; the real HF `VJEPA2Model.forward` requires `pixel_values_videos`. Local suite green, box crashed. | Re-audited `physics_auditor/models/pretrained.py` against `tests/test_pretrained_encoders.py`: the call site (`pretrained.py:131`) and the mock (`test_pretrained_encoders.py:147`) both use `pixel_values_videos` -- convention is correct. `DINOv2Encoder`'s torch.hub convention is unverifiable offline but is exercised end-to-end by an on-box smoke block (`remote_job.sh`) before any expensive cache population runs. | No new guard needed for the current code (already correct); `tests/test_pretrained_encoders.py` pins the convention going forward. `remote_job.sh` keeps its on-box DINOv2/V-JEPA-2 smoke blocks immediately after `pytest -m smoke` + `pytest -q`, before the expensive cache-population steps. |
| C -- environment assumptions in remote shell | `$HOME` unbound under `set -u` in EC2 user-data; the unbound-var abort BYPASSES the ERR trap, so the log-upload trap never fired. Sweep found a SECOND instance of the same bug class: `${BUCKET:?msg}` at the top of `remote_job.sh` ran before the ERR trap / cost-backstop shutdown were registered. | Confirmed via a standalone bash repro that `${VAR:?msg}` aborts under `set -euo pipefail` WITHOUT firing a registered `trap ... ERR` -- identical mechanism to the `$HOME` bug. Also found `launch_gpu.sh`'s EC2 user-data heredoc has no ERR trap of its own, so a `remote_job.sh` download/chmod failure there leaves zero diagnostics in S3. | `scripts/aws/remote_job.sh` now schedules the cost-backstop shutdown and registers the ERR trap BEFORE the `BUCKET` check, and the check itself is a normal `if`/`false` (not a `${VAR:?}` expansion) so it triggers the trap like any other failure. The user-data heredoc in `launch_gpu.sh` now has its own `trap ... ERR` that uploads `cloud-init-output.log` on failure. Guards: `tests/test_remote_job_ordering.py`, `tests/test_shell_scripts_syntax.py`. |
| D -- stale/duplicated external identifiers | `launch_gpu.sh` used a nonexistent SSM parameter path for the AMI (fixed previously by switching to `describe-images`); only failed at launch time. Sweep found the account id is hardcoded independently in TWO files with no cross-check, and the instance-type/vCPU quota is never pre-flighted. | `ACCOUNT_ID="652742769396"` was duplicated verbatim in `launch_gpu.sh` and `pull_results.sh`; each independently derived `BUCKET` from it, so a drift between the two literals would silently target different S3 buckets. No pre-flight existed for AMI resolution, bucket reachability, or instance-type availability before the (expensive) IAM/bucket-creation steps in `launch_gpu.sh`. | `scripts/aws/_env.sh` is the single source of truth for `PROFILE`/`REGION`/`INSTANCE_TYPE`/IAM/SG names; `ACCOUNT_ID` is derived live via `aws sts get-caller-identity` in both `launch_gpu.sh` and `pull_results.sh` (never hardcoded). `launch_gpu.sh --preflight` cheaply verifies AMI resolution, bucket reachability, and instance-type region-offering WITHOUT creating any resources. Guard: `tests/test_aws_identifiers.py`. EC2 vCPU service-quota pre-flight is documented as a follow-up in `scripts/aws/runbook.md` but not yet automated (requires a quota-code lookup call this sweep did not have credentials to validate against a real account). |
| E -- tests depending on untracked local state | `tests/test_report_card_e2e.py` required `artifacts/weights` (gitignored) -- green locally, red on any fresh clone; aborted a GPU run before the sweep's fix. | Re-verified via a real fresh-clone simulation (`git archive HEAD` into a scratch dir, no `.venv`/`artifacts/`/`cache/`): 222 passed, 1 skipped (the same test, now cleanly skipped via its `skipif`). No other test file references a literal `artifacts/` or `cache/` path outside `pytest`'s `tmp_path` fixture. Follow-up review found a second instance of the SAME class: this session's own new guard/smoke files (this doc, `scripts/fresh_clone_smoke.py`, all `tests/test_*.py` guards) are themselves untracked, so `git archive HEAD` -- which `fresh_clone_smoke.py` uses to simulate a fresh clone -- silently omits them, and the script failed with an opaque `FileNotFoundError` deep inside pytest rather than explaining why. | `tests/test_guard_untracked_state.py` statically enforces that any test file referencing `"artifacts/"` / `"cache/"` also carries a `skipif` guard, and pins the exact guard condition on `test_report_card_e2e.py`. `scripts/fresh_clone_smoke.py` is the standing pre-GPU-launch gate: `git archive HEAD` -> scratch dir -> `uv run pytest -m smoke`; it now checks `git ls-tree -r HEAD` up front for `tests/test_smoke.py`/`tests/test_smoke_torch.py` and fails loudly with an actionable "commit these files first" message instead of a confusing downstream error (`tests/test_fresh_clone_smoke.py`). **This session's new files (this doc + all new `tests/test_*.py` guards + `scripts/fresh_clone_smoke.py` + `scripts/aws/_env.sh`) are still uncommitted as of this fix -- per this task's "no git" hard rule none of this was committed automatically; Sam needs to review and commit them before any of this protection is actually in force for a real GPU dispatch.** |
| F -- browser-only ordering/hoisting bugs | `docs/gallery/index.html` rendered fine and passed every static check, but threw an intermittent uncaught `TypeError` at page load (`updatePlayhead` reading an undefined registry entry) because `loadImages()`'s `Image.onload` could fire synchronously before `drawTraceChart()` (which populates that registry) had run. Reproduced via live execution in Chrome (claude-in-chrome), not via any static tool. | Confirmed the root cause: inside `initPlayer()`, `loadImages()` was called before `drawTraceChart(...)`. `updatePlayhead()` already had a defensive `if (!reg \|\| !playhead) return;` guard, which is why the bug was non-fatal (page recovered) rather than breaking the tab outright. | Fixed the ordering in `docs/gallery/index.html` (`drawTraceChart` now runs before `loadImages()`). Guard: `tests/test_gallery_js_ordering.py` -- pins the ordering, the defensive guard, and (node is available in this environment) a `node --check`/Function-constructor syntax floor. Runbook note: gallery HTML changes still need a live browser pass (see `scripts/aws/runbook.md`) -- this class of bug is not fully catchable statically. |
| J -- host-dependent tests: suite green on CPU-only dev machine, red on the GPU box | 5 tests in `tests/test_pretrained_encoders.py` passed locally but FAILED on the real T4 GPU box (`torch.cuda.is_available()` True there): `test_dinov2_resize_normalize_pipeline_correctness` (`RuntimeError: mat1 on cpu vs tensors on cuda:0`), `test_dinov2_encode_clip_cached_round_trip`/`test_vjepa2_encode_clip_cached_round_trip` (cache file never created -- `encode` died on device mismatch before `np.save`), `test_vjepa2_spatial_mean_pipeline_correctness` (`RuntimeError: two devices cuda:0 and cpu`, an fp16-on-cuda tensor mixed into the cpu-mock math), `test_vjepa2_encode_batch_default_batch_size_is_1_on_cpu` (asserted `device == "cpu"`, got `"cuda"`). | Root cause confirmed in `physics_auditor/models/pretrained.py`: `_device_plan()` is the sole `torch.cuda.is_available()` check for the module and silently returns `("cuda", True, ...)` whenever it's called on a cuda-capable host -- but every one of the failing tests calls `enc.encode(clip)` (which internally calls `_device_plan()`) while injecting a cpu-only mock `nn.Module`/tensors, an assumption that only holds on a machine WITHOUT a GPU. Locally reproduced the box's failure class by temporarily forcing `torch.cuda.is_available()` to return `True` via a scratch `tests/conftest.py`: since local torch is a `+cpu` wheel, the same 5 tests failed with `AssertionError: Torch not compiled with CUDA enabled` / a `device=='cpu'` assertion mismatch instead of the box's device-mismatch `RuntimeError`s -- different surface error, same host-dependence, confirming the bug is reproducible without needing the real box. Swept the rest of `tests/` for the same class (grep for `cuda`/`is_available`/`device=` across every test file): `tests/test_remote_job_ordering.py` and `tests/test_pyproject_gpu_index.py` also reference `cuda`/`is_available`, but only as static text/config checks (`grep` for `torch.cuda.is_available()` inside `remote_job.sh`'s source text, and a `tomllib` parse of `pyproject.toml`) -- neither actually calls or depends on the real `torch.cuda.is_available()` return value, so neither is host-dependent. No other test file references cuda/device at all. Also re-ran the FULL suite once under the same global `is_available()->True` conftest hack: found 14 further failures (`test_predictor.py`, `test_torch_encoders.py`, `test_stacks.py::test_adapter_surprise_runs_on_real_cached_pair`, `test_smoke_torch.py::test_pretrained_encoders_construct_and_mock_encode`), all with the identical traceback shape `RuntimeError: Tried to instantiate dummy base class _cuda_isCurrentStreamCapturing` from deep inside `torch.optim.adam`'s own internal CUDA-graph-capture health check. Investigated: none of those files or the production code they exercise (`predictor.py`, `encoders.py`) reference `cuda`/`device` at all -- they never move a tensor off cpu. The failure is an artifact of forcing `is_available()` to lie True on a `+cpu`-only local wheel: PyTorch's own optimizer internals probe the real CUDA runtime once `is_available()` claims True, and a `+cpu` build has no real CUDA runtime behind that lie, so the probe itself throws. On the actual GPU box, `is_available()` is genuinely True against a real `cu130` wheel with a real CUDA runtime, so this probe would simply succeed -- confirmed a false positive of the simulation method, not a real host-dependency bug, and out of scope per "test-assumption bugs only." No changes made to those files. | Added an autouse `_pin_cpu_device_plan` fixture at the top of `tests/test_pretrained_encoders.py` (`monkeypatch.setattr(torch.cuda, "is_available", lambda: False)`), so every test in the file runs the CPU path it was actually designed to pin, regardless of the host's real GPU. Added `test_device_plan_cpu_when_cuda_unavailable`/`test_device_plan_cuda_fp16_when_cuda_available` as direct unit tests of `_device_plan()` itself (previously only exercised indirectly). The real cuda path stays covered by `remote_job.sh`'s fail-fast cuda gate (class I) plus its on-box one-clip DINOv2/V-JEPA-2 encoder smoke blocks (class B) -- this suite only needs to pin the CPU-equivalent math. This sweep (docs/failure-sweeps.md) is itself the guard against a future test file reintroducing this class undetected. |
| G -- long jobs must be resumable + redundant recompute | A GPU run was deliberately killed after measuring V-JEPA-2 encode at ~40s/clip fp32 unbatched on a T4 -- 672 clips x 2 encoders would have needed 10+ hours. The job had ALSO wasted ~2h re-encoding the three LOCAL stacks (raw-pixel/raw16-v1, tiny-cnn-ae, tiny-cnn-pred; 672 files each) even though `scripts/train_stacks.py` was invoked with `--stacks dinov2-s14,vjepa2-vitl`. Worse: nothing synced partial cache progress to S3 during the run, so everything encoded before the kill would have been lost had a manual `aws s3 sync` loop not been bolted on ad hoc via SSM mid-run. Everything encoded so far (2,290 files) was preserved at `s3://physics-auditor-652742769396/results/cache-partial/` with layout `cache-partial/<encoder_cache_key>/<scenario_id>.npy` (dirs: `dinov2-s14-74d24787` 132 files, `vjepa2-vitl-960e266e` 131 files, plus the 3 complete local-stack dirs). | Audited `scripts/train_stacks.py`'s stack-selection logic end to end: the `--stacks` filter was in fact applied correctly at every gate (`if "raw-pixel" in requested`, etc.) -- but this logic lived inline in `main()` with no pure, independently-testable function backing it, so there was no regression guard against a future edit silently reintroducing exactly this bug, and the only way to have caught a regression would have been a real (slow, GPU-only) run. Separately, confirmed `models/cache.py`'s `encode_clip_cached` already skips existing `.npy` files unconditionally -- pre-seeding `cache/` from a prior partial run makes the job naturally resumable for free, nothing in `cache.py` needed to change. Also confirmed `remote_job.sh` had no built-in partial-sync mechanism at all -- the mitigation used last run (a manual `aws s3 sync` loop bolted on via SSM) was never captured in the script itself, so it would not exist on the next dispatch unless someone remembered to redo it by hand. | Extracted the stack-selection logic into a pure function, `plan_stacks(requested) -> list[str]`, in `scripts/train_stacks.py`, and rewired `main()` to consult it exclusively -- guarded by `tests/test_train_stacks.py` (`test_plan_stacks_pretrained_only_excludes_all_local_stacks` pins the exact incident shape: `--stacks dinov2-s14,vjepa2-vitl` must plan zero local-stack work). `scripts/aws/remote_job.sh` now (1) pre-seeds `cache/` from `s3://$BUCKET/results/cache-partial` immediately after repo untar and before any encoding step, so a resumed job re-downloads and re-encodes nothing already cached; (2) starts a background `aws s3 sync ./cache s3://$BUCKET/results/cache-partial` loop (every `SYNC_INTERVAL_S`, default 600s) right after the ERR trap/backstop are armed and `BUCKET` is validated, so even a mid-run kill preserves everything encoded up to the last sync; (3) both the success path and `on_error` call a shared `stop_partial_sync_loop` (kill the loop + one final sync) before shutdown, so the very last minutes of work are never lost to the sync interval's own lag. Guards: `tests/test_train_stacks.py`, `tests/test_remote_job_ordering.py` (`test_partial_sync_loop_starts_after_err_trap_and_bucket_check`, `test_partial_sync_loop_precedes_repo_download`, `test_cache_preseed_precedes_any_encoding_step`, `test_stop_partial_sync_loop_called_in_on_error_and_success_path`). V-JEPA-2/DINOv2 throughput itself was separately addressed in `physics_auditor/models/pretrained.py`: `.load()` now casts the model to fp16 on cuda, and `VJEPA2Encoder.encode_batch` batches multiple clips per forward (default batch_size 4 on cuda, 1 on cpu) -- see `tests/test_pretrained_encoders.py`. |
| H -- CRLF / platform line-ending divergence | Shell scripts under `scripts/aws/` were suspected of being committed with CRLF line endings; they ran fine locally via Git-Bash/MSYS but broke a GPU run on Linux with `syntax error near unexpected token '{\r'`. | A byte-level sweep of every tracked `.sh` file's actual git blob (`git show HEAD:<path>`) found zero `\r` bytes in any of them -- the committed history was already LF-clean. The `\r` bytes actually observed locally (in the working-tree copies of `scripts/aws/*.sh`, plus stray `\r` in `docs/gallery/gallery_data.js` and `scripts/run_mechanistic.py`) were reproduced as an artifact of this machine's `core.autocrlf=true`, which smudges LF blobs to CRLF on checkout; `git add --renormalize .` after stripping `\r` from the working tree produced zero staged diff against `HEAD` for the content files, confirming the blobs were unaffected all along. The risk is real even though this repo's current history is clean: any contributor with `core.autocrlf=true` (or an editor that injects CRLF) can commit a CRLF `.sh` file that passes `bash -n` locally under MSYS -- which tolerates `\r` -- and only fails on Linux. | A root `.gitattributes` normalizes line endings per file type regardless of a contributor's local `autocrlf` setting (LF for `.sh`/`.py`/`.md`/`.toml`/`.js`/`.html`, binary passthrough for `.png`/`.npy`/`.pt`). `tests/test_shell_scripts_syntax.py::test_no_carriage_returns_in_tracked_shell_scripts` asserts every tracked `.sh` file's raw bytes contain no `b'\r'`, platform-independently -- it does not rely on `bash -n`, which is exactly the check that MSYS's tolerance made unreliable. |
| I -- silent CPU torch wheels on the GPU box | `pyproject.toml` pinned the PyTorch CPU wheel index GLOBALLY via a bare `[[tool.uv.index]] url = ".../whl/cpu"` (added for Windows dev, where no CUDA wheel is relevant). `uv sync --group gpu` on the GPU box honoured this index just as much as local Windows dev, installing `torch==2.12.1+cpu` on a CUDA-capable Linux instance -- no error, no warning. `torch.cuda.is_available()` silently returned False and encoding fell back to CPU, ~20-40s/clip instead of the CUDA budget -- the same throughput class as before class G's fp16/batching fix, just from a different root cause. Separately, `remote_job.sh` ran `scripts/run_report_card.py`/`scripts/run_mechanistic.py` to warm the mechanistic-probe latent caches (seeds 300..331 train, 400..415 test) -- both crash on the box with `FileNotFoundError` on `artifacts/weights/predictor_dinov2-s14.pt`, since predictor weights are only ever trained locally, never on the GPU box. | Confirmed the index had no `explicit = true` and no `tool.uv.sources` scoping -- `uv` picks an index implicitly by default, so any platform running `uv sync` against this index config resolves torch/torchvision from it, CPU wheel included. Confirmed PyPI's own linux x86_64 torch/torchvision wheels are CUDA-enabled, so scoping the CPU index to Windows-only and leaving Linux to fall through to PyPI is sufficient -- no second GPU-specific index needed. Audited `run_report_card.py`/`run_mechanistic.py`: both load `artifacts/weights/predictor_*.pt` unconditionally near the top, and that directory is gitignored/local-only -- exactly the class-E untracked-local-state shape, but for a script invoked as an on-box cache-warming step rather than a test. The mechanistic-probe latent generation those scripts incidentally did (via `physics_auditor.probes.mechanistic.data.probe_pairs`) has no dependency on trained predictor weights at all -- it only needs the encoder + the on-disk clip cache. | pyproject.toml: the CPU index is now `explicit = true`, and `tool.uv.sources.torch`/`.torchvision` route to it only under `marker = "sys_platform == 'win32'"` -- on Linux, torch/torchvision resolve from PyPI's CUDA-enabled wheels instead. Guard: `tests/test_pyproject_gpu_index.py` (`@pytest.mark.smoke`) parses `pyproject.toml` with `tomllib` and asserts both the `explicit = true` and the Windows-only marker, so a future edit can't silently reintroduce a global CPU pin. `scripts/train_stacks.py` gained a pure `probe_clips()` function (4 laws x 2 splits x pairs x 2 = 384 clips) and a `--probe-caches` flag that pushes them through the existing `encode_clips_cached` batched path alongside train/val/eval -- guarded by `tests/test_train_stacks.py`. `remote_job.sh` now (1) runs a fail-fast cuda gate (`assert torch.cuda.is_available()`) immediately after `uv sync --group gpu`, so a CPU-wheel regression aborts in seconds instead of silently running 20x slower for hours; (2) warms probe caches via `train_stacks.py --stacks dinov2-s14,vjepa2-vitl --probe-caches` instead of the crashing `run_report_card.py`/`run_mechanistic.py` calls (removed); (3) no longer tars/uploads `artifacts.tar.gz`, since nothing on the box produces `artifacts/` any more. Guards: `tests/test_remote_job_ordering.py` (`test_cuda_gate_after_uv_sync_and_before_train_stacks`, `test_train_stacks_line_uses_probe_caches_flag`, `test_report_card_and_mechanistic_scripts_no_longer_invoked`, `test_artifacts_tar_no_longer_produced_or_uploaded`). |
| K -- host-OOM from unbounded window materialisation + silent stdout buffering | A GPU run (g4dn.xlarge, 16GB host RAM) died with exit 137 (host OOM killer) during the causal V-JEPA-2 cache-warm. `models/cache.py`'s `encode_clips_cached` passed ALL uncached clips to `encoder.encode_batch(...)` in one call; `CausalVJEPA2Encoder._encode_core_batch` in `models/pretrained.py` materialised EVERY resized window for EVERY clip up front into `flat_windows` (each (WINDOW,3,256,256) float32 CPU tensor ~= 12.6MB for w16 / 25MB for w32) before running any forward -- 128 train clips x 48 windows x 12.6MB ~= 77GB. Separately, the job printed nothing for 105 minutes (python stdout block-buffered to a log file), so the OOM gave zero warning before the kill. Also: causal encodes cost 16x/32x the plain stack per clip, and the full 1504-clip fan-out for both causal stacks was ~27h on a T4 -- impossibly over the 420-min backstop, so the causal stacks needed scoping down to only what the pre-registered gate actually needs. | Confirmed the exact shape: `_encode_core_batch`'s `flat_windows` list comprehension ran to completion (building every clip's every window) before the chunked `while i < n` forward loop ever started, so peak resident memory was the full fan-out regardless of `batch_size`. The single-clip `_encode_core` path had the identical bug (`xs = [...]` built for every frame up front). `encode_clips_cached` had no per-chunk print at all -- a single `encoder.encode_batch(uncached_clips, ...)` call for however many clips were uncached, with no intermediate flushed output. | `CausalVJEPA2Encoder._encode_core`/`_encode_core_batch` (`physics_auditor/models/pretrained.py`) now walk (clip, frame-window) pairs lazily: raw uint8 windows are built cheaply up front (`_build_windows`), but the expensive resize/normalise to float32 only happens for the CURRENT chunk of <= `batch_size` windows, immediately before the forward that consumes them -- peak resident resized-tensor memory is now bounded by `batch_size` regardless of total clip/window count. `models/cache.py`'s `encode_clips_cached` now chunks its own `encode_batch` calls at 25 clips per call, printing `  [cache] {cache_key}: {done}/{total} uncached clips encoded` with `flush=True` after each chunk -- real progress visibility even under block-buffered stdout, and a mid-encode kill now loses at most 25 clips of work (better resume granularity too, since each chunk's `.npy` files land on disk before the next chunk starts). `scripts/train_stacks.py` gained `--causal-scope` (default `"voe"`): causal stacks (name starting `"vjepa2-vitl-causal"`) are now encoded only against `voe_clips()` (train + val clips + eval pairs for permanence and permanence-ext ONLY, never probe clips even with `--probe-caches`) unless `--causal-scope full` is passed; non-causal stacks are unaffected. Guards: `tests/test_pretrained_encoders.py` (`test_causal_vjepa2_encode_batch_streams_windows_never_materialising_more_than_batch_size`, `test_causal_vjepa2_encode_streams_windows_never_materialising_more_than_batch_size`), `tests/test_cache.py` (`test_encode_clips_cached_prints_flushed_progress_every_25_clips`), `tests/test_train_stacks.py` (`test_causal_scope_voe_encodes_train_val_and_permanence_pairs_only`, `test_causal_scope_full_preserves_legacy_behaviour`, `test_causal_scope_does_not_affect_non_causal_stacks`, `test_causal_scope_mixed_stacks_each_get_correct_clip_set`). |

## Smoke tier (split: `smoke` <=10s torch-free + `smoke_torch` unbounded)

Profiling found `import torch` alone costs ~15s cold in this environment
(confirmed by timing `uv run python -c "import torch"` standalone) -- not
anything in the smoke tests' own logic, which runs in well under 1s
combined once the interpreter is up. That makes any single tier that needs
torch structurally incompatible with a <=10s budget here, so the tier was
split in two:

- `tests/test_smoke.py` (`pytest -m smoke`) -- TORCH-FREE. Imports every
  torch-free `physics_auditor.*`/`scripts/*.py` module (a `TORCH_MODULES`
  set excludes the rest and is itself pinned against drift), runs one
  reduced-config minimal-pair generation, runs VoE against a deterministic
  stub adapter, renders a report card, exercises the monitor's threshold
  math on synthetic scores, and calls the gallery data builder's pure
  functions on synthetic input. **Measured: `uv run pytest -m smoke -q
  tests/test_smoke.py` runs in ~2s pytest-internal / ~5s wall including `uv`
  + interpreter startup -- within the <=10s hard rule.**
- `tests/test_smoke_torch.py` (`pytest -m smoke_torch`) -- the torch-backed
  counterpart: imports `physics_auditor.models.{encoders,predictor,
  pretrained,stacks}` and the torch-using `scripts/*.py`, constructs +
  mock-encodes every encoder class, checks predictor rollout shape. Not
  held to a time budget (pays the one-time torch import cost, ~16s
  measured), but still mock-injected only -- no network/`.load()` calls.

Both markers are registered in `pyproject.toml`. `scripts/aws/remote_job.sh`
runs `-m smoke` then `-m smoke_torch` back to back before the full `pytest
-q`. `scripts/fresh_clone_smoke.py` (the pre-GPU-launch gate) runs `-m
smoke` only, and now measures its own wall time and fails if it exceeds the
10s budget in the fresh-clone export, not just checking the exit code.
